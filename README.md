# Supabase Anonymous user store

To get started, start the instance with `npx supabase start`.
Then, run the project with `npm run dev`.

## Pre-existing user accounts

Mail: dev@supa.base, Password: 1234
